import { OutOfStockDirective } from './out-of-stock.directive';

describe('OutOfStockDirective', () => {
  it('should create an instance', () => {
    const directive = new OutOfStockDirective();
    expect(directive).toBeTruthy();
  });
});
